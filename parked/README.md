# parked/ — références figées (31/07/2026)

Snapshot de l'engine ASM v2 et des exemples AVANT la migration 1:1 depuis la
v1 (voir `.claude/skills/v1-migration/SKILL.md`). Ces fichiers servent de
référence pour récupérer les spécificités v2 pendant la migration.

**Ne pas modifier. Ne pas inclure dans un build.**

- `parked/engine/`   : l'engine v2 pré-migration (dialecte normalisé v2)
- `parked/examples/` : les exemples pré-migration (appellent le dialecte v2)

Les exemples migrés reprennent leur place dans `examples/` au fur et à mesure.
