; La boule de feu de la proue — GENERE par tools/gen_warship_frontmulti.py

; l'eclat de bouche (1000:7DB6), 8 recettes, index (vie & $0E)/2
muzzle.Sets
        fdb   set_fireball_flash_0
        fdb   set_fireball_flash_1
        fdb   set_fireball_flash_2
        fdb   set_fireball_flash_3
        fdb   set_fireball_flash_4
        fdb   set_fireball_flash_5
        fdb   set_fireball_flash_6
        fdb   set_fireball_flash_7

; (pas de dissipation dediee — voir l'en-tete du generateur)

; la boite (1000:7E4E)
fireball.BOX equ $0306
fireball.CTR equ $0000
