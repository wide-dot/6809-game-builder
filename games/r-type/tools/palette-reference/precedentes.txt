# Tables de communs SUPERSEDEES, une par ligne, 12 triplets hexa.
#
# A quoi ca sert. `palette_migrate.index_migres` repond « ce fichier est-il
# deja renumerote ? » en comparant ses 13 premieres entrees a la nouvelle
# palette. Le jour ou l'auteur regle la valeur d'un commun, tous les fichiers
# deja migres portent l'ANCIENNE valeur et se declarent d'un coup non migres :
# le rejeu s'arrete au groupe A sur des index « sans correspondance », tres
# loin de la cause. Vecu le 17/08 sur les deux rouges.
#
# La question posee est celle de l'ORDRE des index, pas de la valeur exacte
# d'une couleur. On garde donc l'egalite — exacte, sans approximation qui
# ferait basculer les fichiers ne portant aucune des deux tables (mesure : un
# critere de distance en reclassait 51 a tort) — mais on l'accepte contre la
# table courante OU l'une de celles ci-dessous.
#
# REGLE : regler un commun dans nouvelle.png, c'est ajouter ici la ligne qu'on
# quitte. Sinon le rejeu casse au prochain passage.
#
# 17/08/2026 — les deux rouges purs rapproches de l'arcade, sur demande de
# l'auteur : 610000 -> 580000 et AC0000 -> 880000. Les deux nouvelles valeurs
# tombent exactement sur des barreaux de la rampe arcade (88,0,0 et 136,0,0).
000000 616161 A8A8A8 FAFAF2 00618F 009ECC 08D4EB 610000 AC0000 CC5A3C F99B68 FAF261
